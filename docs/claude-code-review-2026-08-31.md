# Claude Code 代码审查记录（2026-08-31）

当前状态：已完成部分审查与修复，尚未取得全范围复审通过结论，不能宣称整仓无问题。

## 上游恢复后的进展（2026-09-01）

用户恢复推理链路后，本任务只修改私有审查适配器，要求每次显式传入
`--model claude-gpt-5.6-sol`。这是本次用户指定的 GPT 5.6 Sol 网关别名，不是 Anthropic
Opus；Claude Code 仍是执行客户端。真实只读 smoke 正确读取 `AGENTS.md` 并返回结构化
结果，CLI 用量元数据报告 200,000 上下文窗口，不沿用旧 1M 假设。健康及模型预检观测
已刷新；审查阶段没有修改授权、登录或网关配置。后续本机部署另见下方记录。

- 活动事务核心：真实 5 次 Read、约 33 秒，返回限定范围 `REVIEW_VERDICT=clean`。
  主 Agent 另行核对私有事务驱动、锁、版本及回滚路径；不涵盖独立 actual/outcome API。
- 任务准备与角色记忆 CAS：真实 7 次 Read、约 88 秒，返回限定范围 `REVIEW_VERDICT=clean`。
  主 Agent 另行核对 Node → Provider 的准备调用、持久任务目标和记忆写入；成功 prepare
  仍是记忆提交点，不承诺后续知识提炼与 complete 同事务。
- 原生 worker 只读核实两条旧运行时候选，主 Agent 补回归后均先复现失败，再完成修复：
  最后一个租约释放后移除外部执行器的临时运行条目；语义异常的成功租约响应明确报错，
  不启动受保护操作，保留有效返回 ID 时先尝试回收。明确的远端关闭响应仍兼容。
- 运行时第一轮复审又发现 `{enabled:false}` 缺少 ID 仍被宽松 null 判断接受。主 Agent
  新增用例，先复现未拒绝，再改为显式 `leaseId === null`；第二轮真实 6 次 Read、
  约 42 秒返回限定范围 clean。34 项相关回归通过，Node 正式全量 689 项中 683 通过、
  6 条件跳过；数据库验收没有在这次 Node 命令中启用。图服务启动和状态存储
  是替身，HTTP/broker 真实；异常响应是测试注入，不宣称当前匹配服务正常会发出该响应。
- 员工 URL 错误编码：通过真实 HTTP 先复现 500，再把路径解码错误归为 400；在委派
  员工端口或重定向前拒绝，保留有效编码与正常 308。相关 30 项回归通过，Claude
  真实 7 次 Read、约 60 秒返回限定范围 clean。员工端口是替身，不代表真实包/数据库验收。
- 旧员工报告的 Host 候选在公共入口不可达：请求策略先校验 Host/Origin，再进入员工
  路由；主 Agent 与本轮 Claude 均核对该顺序。Provider 的 upsert/get 返回完整任职投影，
  Node 映射保留 assignments 并补齐 capabilities；旧报告关于正常重启用丢失历史、正常
  档案缺 capabilities 的前提未成立，不按已确认缺陷修改。非匹配旧 Provider 不在该结论内。
- 任务创建核心：真实 6 次 Read、约 47 秒返回限定范围 clean；涵盖主记录、初始事件、
  参与者、招聘关联及旧记录补边，不包括此前的招聘创建副作用。
- checkpoint 生命周期首轮指出无记忆的已认领检查可用相同指纹追加记忆。主 Agent
  通过公开 Provider HTTP 和真实 Neo4j 先复现四项错误 200，再把首次认领是否包含记忆
  绑定到任务。已完成检查同样拒绝晚追加；旧记录按该 token 的既有记忆检查兼容。
  七项新回归通过；第二轮真实 8 次 Read、约 84 秒返回限定范围 clean。
  成功 prepare 后新轮次替代旧轮次时，旧 token 仍不能 complete，这是保留的提交边界，
  不是已实现整段知识捕获事务或自动回滚。
- 自动选人首轮指出解释没有充分披露工作类型、负载与非语义兜底；主 Agent 先复现
  三项解释断言失败，补齐说明及实际候选数量，不改变选择排序、授权或 reason 机器码。
  第二轮真实 8 次 Read、约 50 秒返回限定范围 clean；不涵盖新角色的前置招聘。
- 本阶段最终验证：Node 正式全量 690 项中 684 通过、6 条件跳过；六项条件验收随后
  在独立真实 HTTP/MCP/Neo4j 环境全部启用并通过。Provider 默认套件 326 通过、41 跳过；
  真实 Neo4j 核心 38 项全部通过，含记忆、创建、活动、选人与新 checkpoint 重放用例。
  Provider 输出仍有七项既有警告，不宣称零警告。所有数据库样本均为合成数据，隔离实例已停止。

没有 ID 或回收请求本身失败时，客户端不能证明远端即时释放，只能报告失败并由远端
TTL 兜底；没有为了测试而读取或修改日常数据库。
全范围审查及普通输入 hooks 产品验收仍未完成，以上七个限定范围 clean 不扩大到全仓库。

### 前置招聘及旧授权接口补充

前置招聘第一轮返回三条候选。取消竞态的初始描述遗漏了活动接口现有 TTL 守卫，
主 Agent 没有直接放宽取消授权；另用真实 HTTP/Neo4j 验证出三个具体缺陷并修复：

- 自动重试的 `OPTIONAL MATCH` 局部 WHERE 没有过滤整行，可立即抢占未过期 claim；
  增加 WITH 后整体过滤，保持 claim/audit 可在 TTL 后复用恢复。
- 已 inactive/archived 的 HR 仍能批准招聘；审批原子守卫现在要求 active HR，取消不受此限制。
- fulfillment 守卫拒绝后，身份与任职已单独提交；现在用请求私有事务同时保存身份、任职
  和 fulfillment，并按 task → recruitment 加锁。失败回滚，外层 requested 审计仍保留。

旧空间级 recruitment-policy 是仍可写的兼容存储，但项目 policy 才是实际授权来源。
主 Agent 和原生只读 worker 独立核对代码、当前 UI 与原有测试后，保留项目规则：旧接口
继续存储原值，同时返回 superseded、不生效及替代工具提示，OpenAPI 标为 deprecated，
Node 不对旧 Provider 伪造元数据，MCP 明确引导使用确切项目的协调策略工具。
这不新增空间策略覆盖，也不改变现有项目授权。

第二轮 Claude Code 完整读取 15 个约定文件，约 104 秒，返回限定范围
`REVIEW_VERDICT=clean`；实际响应模型为 `claude-gpt-5.6-sol`。它未运行测试，
主 Agent 另取得七项新真实库回归通过；故障注入、虚拟过期时钟与真实并发取消分别标明，
不能把合成角色当成真实人员或产品客户端验收。累计八个范围 clean，仍不是整仓 clean。

本阶段正式 Node 全量 691 项中 685 通过、6 条件跳过；Provider 默认 326 通过、48 跳过。
Provider 原有跨空间测试因新增 claim 查询而取错查询位置，改为按 fulfillment 语义定位，
完整保留原有空间隔离断言，并新增 claim 查询的空间与 claim ID 断言；未放宽实现门禁。
候选包 Node 25 项及真实数据库核心 45 项通过，隔离数据库已停止。
Claude 建议的额外覆盖随后由原生 Codex worker 补齐：在审批 claim 提交后、首次 guarded
provisioning 查询前注入故障，再由真实 activity HTTP 验证未过期取消返回 409、任务和招聘
快照不变、角色仍不存在；HR inactive/archived 两种状态下的取消均返回 200、不创建角色。
HTTP 主体仍是夹具认证主体，不冒充停用 HR 本人。该文件 10 项通过；主 Agent 独立复跑
整组真实库核心 48 项通过，Provider 默认 326 通过、51 条件跳过，保留七项既有警告。
最终候选还通过六项真实 HTTP/MCP/Neo4j 接续验收；所有临时实例已停止。

### 独立 actual/outcome 接口：复审闭环已完成

Claude Code 首轮完整读取 12 个约定文件，约 137 秒，返回 `issues_found`，实际响应
模型 `claude-gpt-5.6-sol`。主 Agent 不将其所有候选直接作为结论；已通过隔离 Neo4j 与
公开 HTTP 复现并修复以下问题：

- 迟到 actual 覆盖当前运行和更新时间；现在历史与当前投影分开，旧关联观察可恢复时间基线。
- 独立 actual 绕过客户端限制，以及权限读取到写入间的撤权竞态；现在使用请求私有事务，
  按 task → Agent → executor → permission 加锁，授权修改先锁 executor，避免相反锁序。
- 独立评价、忽略、重置在统计失败时遗留部分写入；现在按统计组串行，事实和统计一起回滚。
- ignore 同键不同原因可覆写原原因；现在校验完整载荷，重放不改变首次忽略时间。
- 无时区评价导致汇总失败；新增 actual/outcome/reset 时间要求时区，旧评价仍可读取和忽略。
- 优先级 PATCH 请求模型缺少 personal_space_id；原生 worker 以实际路由和 store、替身 driver
  先复现 422 再修复，主 Agent 独立复跑确认。
- outcome 可归属到无关 Agent、执行器、任务类型、策略和 run，或自称不存在的系统终态。
  七个真实 HTTP 失败用例先复现错误 200；新增关联校验后必须对应已记录执行。可省略 run
  的显式反馈仍保留，但不能给未记录执行记分；系统终态绑定活动事件及 canonical 幂等键。

原生只读 worker 另检查公开调用契约；可选 run 不代表可虚构执行器归属。外部测试引用和
用户反馈仍是调用方提供的声明，本次没有新增证明用户身份或外部测试真实性的认证机制。
后续交错投影测试又先复现活动入口局部覆盖更新的实际执行，改为 actual 接口统一维护
这些投影字段，更新时间不倒退；canonical 终态换键重复记分也已覆盖。阶段性真实库核心
70 项、相关单元 26 项、Provider 默认 327 项通过，73 项条件跳过；Node 全量命令通过。
第二轮 Claude 真实 17 次 Read、约 127 秒仍返回 issues_found：具体上报模型虽然可用，
却可能不满足任务模式、推理强度或能力提示。三个真实 HTTP 用例均先复现错误 200，
现已复用路由的具体模型兼容检查；独立 actual 写入后故障的整体回滚另加测试。
第三轮针对同次运行的旧投影基线污染给出有效问题：旧 projection 的时间被误当成当前观察的
比较基线，可能让新 actual 无法成为当前投影。主 Agent 补回归并修复为只从更早的关联观察恢复
旧基线；第四轮 Claude Code 完整复查约定范围后返回 `REVIEW_VERDICT=clean`。因此本节取得
限定范围 clean，但不扩大为整仓结论。是否进入运行实例仍须以安装后验证为准。

### 任职路由、Node 映射与部署流水线收口

- Provider 任职路由首轮发现 `OPTIONAL MATCH` 会让空任职继续通过，已改为强制匹配目标项目的
  active assignment 并在 Python 层 fail-closed。随后一次静态意见把 work kind 误当绝对门槛；
  真实 Neo4j 证明这会拒绝由能力筛选合法选中的任职。最终合同为：显式 assignment 只校验该
  任职；未传 assignment 时才要求 work kind。第九轮 Claude Code 复审返回 clean。
- Node 映射首轮发现执行摘要会把 configured/display executor 合成为 actual executor，已改为
  只映射真实 actual 字段；第二轮复审 clean。相关 41 项回归通过。
- 部署流水线首轮发现六类边界：新增 direct-call 文件可漏装、未打包差异可被静默忽略、wheel
  边界过宽、Codex 全局指令未纳入客户端回滚、连接状态未设门禁、夹具超时可能挂起。修复后要求
  精确文件白名单、严格 wheel 顶层、配置摘要与权限校验、连接成功门禁、TERM→KILL 有界清理；
  第二轮复审 clean。实际打包又发现 `pip --no-index` 在缺少已缓存构建后端时会在安装前失败；
  改用 uv 的隔离 PEP 517 构建，删除旧 wheel 仍限制在当前事务候选的 artifacts 目录，随后严格
  verifier 通过。第三轮 Claude Code 对这一单文件修复复审 clean；构建期后端解析不等于运行时
  依赖升级，也不把打包成功写成安装成功。
- 最终主 Agent 回归：Node 699 项中 693 通过、6 条件跳过；Web 类型检查通过，54 个文件、
  215 项通过；Provider 默认 333 项通过、86 条件跳过并保留 9 项已知警告；可丢弃真实 Neo4j
  Python 套件 83 项通过，Node 条件套件 6 项通过。所有角色、执行器、模型、会话和故障均为
  合成样本；Neo4j 引擎是真实实例，不能据此冒充真实客户、模型执行或普通输入 hooks 验收。

### 第三次限定本机更新

已与并行任务协调重启窗口，先备份程序和客户端配置，再停止相关服务，对离线数据库树
做完整复制及 SHA-256 校验。此次只更新五个实现文件和角色记忆说明，保留已安装 Web、
依赖清单、图连接、Provider 环境及其他客户端资源；没有覆盖 Jefa 包、配置或数据。
本地 npm 包的 287 文件和 wheel 的 67 个 Python 文件逐项验证后安装，未发布到仓库或注册表。

候选包核心 Node 回归 36 项、候选真实数据库 17 项通过。直接在发布包内运行整仓套件
不是成功验收：包不包含 Web 源码、acceptance、工作流等开发资产，另外四项角色上下文
用例依赖 cwd 对应项目名；保持候选代码、从项目仓库 cwd 重跑该组八项全部通过。
工作区正式全量另行重跑，690 项中 684 通过、6 条件跳过。

三端 Fuli 配置已刷新，服务已恢复；23 项安装检查全部通过，包含 200 个 Node/Web 文件
及 67 个 Provider 文件与限定候选一致、非 Fuli 配置保留、新 MCP 工具/记忆 schema、
在线 Provider 健康及控制台构建。未修改 hooks 信任；配置 current 不证明既有会话已重连。
私有备份、候选包和完整安装报告保留在本机，不将机器路径或配置内容提交到项目文档。

另一已有任务报告了 blocked 任务跨宿主会话恢复的问题：原创建幂等键重放被不同输入
拒绝，blocked → queued/running 也被拒绝。该反馈尚未由本任务独立复现，保留为恢复 API
待办；不能靠放松来源指纹、假报 actual 或修改授权来伪造恢复成功。

### 第四次限定本机更新：招聘边界

上述招聘修复随后已安装到本机。以第三次安装为基线，仅覆盖招聘实现、任务模型、路由、
Node 映射、MCP 定义五个生产文件及角色记忆说明；Web 和依赖清单保持原样。程序/配置
先备份，停止服务后复制数据库树并核对 SHA-256，再安装逐文件校验过的本地 npm/wheel。
没有发布、提交或推送，没有安装或修改 Jefa 包、配置和数据。

三端 Fuli 配置刷新、服务重启完成，25 项安装检查全部通过：200 个 Node/Web 文件和
67 个 Provider 文件匹配限定候选；离线备份、原设置/图身份/Provider 环境及非 Fuli
客户端配置保留；新 MCP 记忆工具和 schema、在线 Provider/控制台、旧策略接口的
superseded/不生效字段与 deprecated 路由均已核对。完整备份及报告保留在本机私有目录。
检查没有修改信任或测试普通输入；三个安装器结果均为配置 current，不能证明现有会话热重载。

## 收尾复核（2026-09-01）

第二次有备份的本机更新已经完成，23 项安装检查通过。此后只读复查 Codex CLI 0.149.1：
工作区与仓库均显示两项 Fuli hooks 已启用但未信任；没有执行 hook、修改信任或启动模型。
现有客户端进程的重连仍需用户操作，安装器配置未变化的结果不能证明进程已经热重载。

原生 Codex worker 完成限定 Cursor 官方文档及四个仓库文件的静态复核；主 Agent 独立
核对官方配置/MCP 页面及本机 Cursor CLI 的帮助。未找到确证排除其他用户级 MCP 的
严格隔离入口。没有启动 Cursor 模型、自动批准全部 MCP 或修改其他服务器配置。
这是隔离未获证明，不是声称每次运行必然启动所有用户 MCP。

当时 Claude Code 2.1.251 的无副作用预检通过；随后单次真实只读健康 smoke 在 10 次
API 重试后再次返回上游 502，未执行 Read，没有审查报告。进程已结束，健康仍上报
degraded；错误占位不作为实际模型证据。此次没有修改登录、网关或健康门禁，也没有
将健康测试冒充全面复审。该外部条件已经反复出现，继续整库 Claude 审查需先恢复服务。

仍须分别完成：用户审阅信任 Fuli hooks 并重连客户端、真实普通输入的入口/结束/读回验收、
Cursor 的受控客户端验收、明确 Cloud 具体产品及连接边界、恢复后的全范围 Claude 复审。
不以已完成的本机更新缩小原始目标，也不把局部 clean 扩大为整仓通过。

## 已证实并修复

| 问题 | 修复 | 验证 |
| --- | --- | --- |
| 冷启动超过 TTL 时交付的租约已失效 | 资源就绪后才开始 TTL | 公共 HTTP 接口先失败后通过 |
| 唤醒期间关闭仍返回成功租约 | 排队和交付前检查关闭状态 | 公共 HTTP 接口先失败后通过 |
| 角色限制的客户端仍能写记忆 | 记忆写入校验 `allowed_clients`，拒绝时返回 403 | 真实隔离 Neo4j + ASGI HTTP，先 200 后 403 |
| 不同会话复用 token 引发数据库异常 | 保持空间内唯一性，冲突明确返回 409 | 先 500 后 409；原会话可读，另一会话未启动 |
| 重放旧轮次返回已被替代的 token | 旧轮次明确返回 409，当前轮次仍幂等 | 先错误 200 后 409；当前轮次可正常回写 |
| 一次瞬时心跳失败永久停止续期 | 后续心跳继续重试，释放/关闭或远端明确失效才停止 | 公共 HTTP 接口先超时失败后恢复成功 |
| 休眠失败立即反复重试 | 最少等待 30 秒重试，并同步下一次休眠时间 | 虚拟时钟 + 公共 HTTP 接口先失败后通过 |
| 启动失败后错误被清空 | 获取失败时保留错误摘要 | 公共 HTTP 状态接口先返回 null 后返回失败原因 |
| 旧 token 搭配当前 turn 被当作有效重试 | 精确 token 匹配优先，拒绝混用；当前 turn 的新 token 重试仍幂等 | 真实隔离 Neo4j + ASGI HTTP，先 200 后 409；Claude 第三轮确认并撤回“仅文案差异”的旧结论 |
| 客户端关闭后迟到的租约响应重新启动心跳 | 关闭后拒绝获取，等待在途获取结束并释放迟到句柄 | 公共 HTTP 接口先错误成功、后拒绝且远端租约数为 0 |
| Codex bootstrap 与入口 hook 同时要求获取偏好 | 已提供 hook 上下文时不重复 fallback；未提供时保持精确工具入口 | 原有生成/安装接口回归先失败后通过，保持幂等与 1000 字节内限制 |
| 工作类型兜底选择未披露 | 返回 `project_context_fallback` 并指出未匹配的工作类型 | 真实 HTTP 回归先失败后通过；显式能力缺失仍为 unassigned |
| 关闭控制面后托管进程残留 | 等待托管适配器停止，清理其租约；不启停外部宿主 | 真实临时 Node 子进程先残留、后确认退出 |
| 托管进程停止失败后不再清理 | 30 秒后重试，不立即循环 | HTTP/service + 合成时钟先失败后通过 |
| 停止未成功就再次启动同一执行器 | 新获取先确认失败实例已清理，失败时拒绝新启动 | HTTP 回归先重复启动、后拒绝；清理恢复后可启动 |
| 部分启动失败的诊断被清理操作抹除 | 清理资源但保留启动错误，成功重启才清空 | HTTP 状态回归先 null、后保留原始错误 |
| 任务主记录与参与者关系分开提交 | 主记录、决策、初始事件、参与者与招聘关联使用同一事务 | 隔离 Neo4j + HTTP：首条/第二条参与者写入故障均整体回滚 |
| 关系目标消失时静默返回缺少参与者的任务 | 检查实际匹配数量，缺失返回 409 并回滚 | 先错误 200、后 409；幂等重试保留取消状态、参与者和事件 |
| 冷启动期间断开的调用方留下无人管理的租约 | 响应连接已销毁时释放迟到租约 | 真实 HTTP abort 先残留 1 条、后回收为 0 |
| 容器闲置停机反而唤起已停止的引擎 | 停机只 inspect；明确停止时不调用启动器或 Compose | 真实服务逻辑 + 容器边界替身先调用启动器、后不调用；显式 start 仍可唤醒 |
| 并发新增指令使 MCP 说明超过 2 KiB | 压缩措辞，保留管理器/专家/排除项目/改名凭据/真实用量规则 | 2496 → 2048 字节；说明与真实 MCP 协议回归 4 项通过，不放宽门禁 |
| 损坏的 URL 编码租约 ID 返回 500 | 在租约路由转换为明确的输入错误 | PATCH/DELETE 先 500 后 400，已有有效租约未受影响 |
| HTTP 监听失败后运行时未清理 | 启动失败等待 system 与 owned application 关闭 | 真实端口占用、HTTP/service/broker；合成托管 worker 先残留后停止 |
| 显式关闭吞掉托管停止失败 | 等待全部停止结果并抛错，仍清理 owned application；服务暴露可等待 close | 失败适配器注入，错误可观察；异步 application 完成且重复 close 保留结果 |
| 响应发出后清理错误导致重复写响应头 | 已提交或断开的响应不再次发送错误体 | 存储故障注入先产生 ERR_HTTP_HEADERS_SENT，后保持原响应及后续健康请求可用 |
| 历史缺边修复使用取消前的陈旧状态 | 事务内先锁 task，再读取最新状态与终止时间 | 真实隔离 Neo4j + HTTP 先补出错误 blocked 参与者，后保留 cancelled、终止时间和既有事件 |
| 关闭清理失败遮蔽原始启动错误 | 同时保留 startup 和 cleanup 异常，启动错误作为 cause | 真实占用端口 + 停止失败适配器，先丢失 EADDRINUSE 后同时可见两个错误 |
| 活动报错但状态已提交，重试跳过缺失审计 | 活动、actual 观察、结果证据、汇总与临时角色归档共用请求私有事务 | 真实 HTTP/Neo4j 先复现无效 executor 被拒后仍 cancelled；修复后验证及三处数据库写入失败均整体回滚，修正同键可重试 |
| 同一旧任务版本的并发活动多个成功 | 授权后先锁目标 task，再读取版本、状态和事件 | 先复现多个 200；修复后 3 轮每轮 8 个并发请求仅 1 个成功，其余 409；同键并发重放不增加版本或事件 |
| Codex 安装结果丢失 hooks 审核提示 | 保留 `trustReviewRequired`，将宿主专属后续步骤传给通用 CLI 输出；重复安装也不将配置未变视为已信任 | 公开 `fl setup` 入口、临时文件安装：新增与重复安装两项分别先失败后通过；未改用户信任设置 |
| 角色包收据符号链接导致包外文件被覆盖 | 在排除收据摘要前先检查文件类型，拒绝符号链接 | 真实 `fl employee install` CLI 在临时目录先覆盖包外合成文件，修复后退出 1，文件不变 |
| 子目录中的同名收据文件未参与包内容校验 | 仅根目录普通收据文件作为元数据排除；子目录内容正常校验 | 真实 CLI 先把内容变化误报为幂等成功，修复后拒绝并要求 `--replace` |
| 默认系统构造失败后已打开的 SQLite 未关闭 | 系统构造错误进入现有可等待启动清理路径，保持应用所有权规则 | 公开 `createServer`、真实临时 SQLite 与无效配置：先观察到已打开但未关闭，修复后拒绝前确认关闭；不请求 Provider 或启动模型 |
| 检查准备因竞争完成或新轮次被拒，却已提交角色记忆 | Provider 将 prepare 认领与记忆 CAS 放入同一事务，失败一起回滚 | 真实 TCP HTTP/Neo4j 暂停一份已读取响应，两种交错均先复现 revision 0→1，修复后拒绝且保持 0；另验证 CAS、丢响应重试和授权 |

另将角色页面的活动日历整理逻辑提取到 `web/src/features/project-agents/activity-evidence.ts`，
保留既有功能和并发改动，不放宽行数门禁。结构检查、类型检查与原有页面测试均通过。

修改未提交、未暂存、未推送。日常数据库未用于验收。
员工弹窗现有测试也修正了一处误报：只检查项目选择器和可见原生下拉，不把标题设置
自定义控件内的 `aria-hidden` 表单元素当成界面退化；未修改界面实现，13 项弹窗测试通过。
活动写入随后提取为 `store_project_agent_task_activity.py`，私有事务适配位于
`store_transactions.py`；原模块新增锁后达到 1306 行，拆分后重新通过既有 1300 行门禁，
未放宽规则。另用两个真实 HTTP 请求验证：前者在审计写入处挂起、后者独立提交，
前者随后回滚不会撤销后者；仅故障时点由外部数据库驱动替身控制。

## 审查覆盖与限制

- 登录恢复后的 Claude Code 2.1.251 真实只读 smoke 已成功，不再以旧认证失败判断当前状态。
- 第一轮整仓读取在 30 分钟上限结束，未返回最终报告，不能计为通过。
- 角色记忆区块：首轮返回 14 条候选，第二轮确认三项修复并撤销多数无依据候选。第三轮覆盖 9 个文件，确认新增的 token 确定性修复并撤回上轮错误判断；另提四条候选。主 Agent 已修复兜底选择不透明、新任务原子保存和旧缺边修复并发状态；两种事务修改仍待 Claude 最终复审结论。
- 员工包区块：12 个源码/测试/文档文件和 `AGENTS.md`，返回 9 条候选反馈。候选不等于已确认缺陷，仍须核验上层授权、返回模型及实际触发条件。
- 运行租约区块：第三轮实际读取 11 个文件，确认客户端关闭竞态与四项托管生命周期修复；报告写“9 次 Read”，但工具证据为 11 次，以工具证据为准。新增五条候选，另有四项待查。主 Agent 随后通过真实 HTTP 复现并修复断连租约，读取容器启动器确认并修复停机反向唤醒；这两项新改动尚待复审。
- HTTP 边界区块：第一轮实际读取 10 个文件，核对入口策略后给出报告。报告摘要称六项缺陷，`risks` 实有九条且混入现行契约/未验证触发，不按摘要直接累计缺陷数量。主 Agent 已核对 EmployeeError 构造器、错误包装器和监听时序；有效问题继续补公共接口回归，不能视为区块通过。
- 原生子 Agent 只提供模块与测试入口清点，未测试或审查行为；清点不能代替 Claude 审查。其分区草案中的路径仍需逐项核对。
- 另一原生子 Agent 完成宿主入口接线只读核对，发现 bootstrap 和中英文文档仍声称 Codex/Cursor 只有 fallback。主 Agent 已核实并修正源码生成指令、工具说明、README 与验收时序图；未修改用户实际安装配置。该 worker 超出约定的 15 文件读取预算，但未超出仓库只读范围。
- 原生持久化核对 worker 只读 8 个文件，确认跨查询缺边、修复路径和读取后果。主 Agent 自行核对当前驱动的真实提交/回滚实现，补隔离 Neo4j + HTTP 回归后完成事务改动；没有把静态报告当作测试执行。
- Claude 任务事务复审：实际 Read 6 个文件、Glob 2 次；随后出现两次 API 重试，在 900 秒上限结束，未形成结论。没有权限拒绝或登录错误证据，不能记作通过，也不能沿用旧认证阻塞。进程已回收、失败执行记录已上报、租约已释放。
- 原生关闭边界核验：完整读取 AGENTS 与 6 个实现/测试文件，只做静态检查。确认可等待关闭及异常响应收尾；提出三个候选。主 Agent 已复现并修复“清理错误盖住启动错误”；随后以默认应用构造和真实临时 SQLite 证实并修复 system 构造失败的资源收尾；共享图服务是否应随控制台关闭不应未经资源归属核实就判为进程泄漏。
- 历史缺边小范围复审：Claude 正常返回 3 文件报告，但其当时“取消会递归进入 transaction 补丁”的 P1 前提不成立：当时活动写入经 `execute_query` 的单条 Cypher，而不是被替换的 Python `transaction` 方法。主 Agent 重跑 5 项真实数据库任务回归通过；对“取消在补边事务前提交”的覆盖限制予以保留。后续活动事务化时已同步将该测试的重入保护移到嵌套 HTTP 请求之前，未改变业务断言。
- 补充实际活动/创建调用点后的交叉复审读取 4 文件、共 5 次 Read，随后因上游 stream error 退出；未给出最终 verdict，不能计为通过。缺失必填旧字段、招聘计划字段重写等意见仍需可达路径证据，不因为审查标签就计为已确认缺陷。
- 同范围重试最终正常返回 `REVIEW_VERDICT=clean`：实际 Read 4 文件、5 次，响应模型为 `claude-opus-5`。Claude 撤回递归 P1、将缺字段意见降为防御建议，保留锁竞争覆盖限制与招聘候选。该 clean 只限历史缺边修复及指定调用区间，不涵盖活动执行审计、全部任务事务或整仓。
- 真实客户端验收脚本静态核验：原生 worker 确认现有 runner 会启动真实二进制，但 Cursor 带 `--approve-mcps` 且环境仅移除 `CODEX_THREAD_ID`；主 Agent 没有直接运行该分支。当前安装版 Cursor 的 MCP 加载器直接读取用户级 MCP 文件，`CURSOR_CONFIG_DIR` 只覆盖 CLI 配置目录，不能据此宣称 MCP 已隔离。
- 活动事务只读交叉检查：原生 worker 确认先写活动、后验证 actual 及证据的顺序与同键早返风险；主 Agent 自行检查授权、归档和真实驱动后，以公开 HTTP 复现并修复。Claude 首轮实际 Read 7 个文件、8 次，响应模型为 `claude-opus-5`，但在 420 秒上限结束，没有最终 verdict。拆分后的核心重试连续遭遇上游错误，最终明确返回 API 502；未读取源码，响应中的 `<synthetic>` 是错误占位，不是实际执行模型。两次进程均已结束，失败活动与租约收尾已处理，都不计为通过，也不判断为登录失败。
- 自动入口的原生只读复核发现了若干条件性候选。主 Agent 对照调用链和官方文档核实：Cursor 的内部 `callAgentTool` 返回原始对象，Stop 并非错误读取 MCP 包装；正常 Codex 发现流程始终提供 hooks 路径。Cursor prompt hook 不支持同轮上下文注入，仍须依靠 sessionStart 引导的每轮偏好回退，不能添加未定义的返回字段来假装支持。已修复的是 Codex 安装审核提示丢失，不把静态核对当成真实客户端普通输入验收。
- 活动核心第三次小范围重试：本地预检通过，Claude Code 2.1.251 真实进程启动后发生 10 次 API 重试，约 197 秒后再次返回上游 502；未执行 Read，没有最终审查结论或真实响应模型证据。失败活动已记录，进程结束，租约已释放。不能记为通过，也未尝试修改用户的模型网关或登录配置。
- 本机更新后的活动核心第四次小范围重试：重新预检通过，真实 Claude Code 2.1.251 在约 188 秒内重试 10 次后返回上游 502；未调用读取工具，没有审查结论。响应模型仍是错误占位，实际模型、会话标识及 token 用量未报告。失败活动和 worker 终态已记录，进程结束，租约已释放；本次没有重试登录或改动网关配置。
- 负责人能力筛选的原生静态交叉核对：自动复用先过滤全部显式能力，再排序工作类型、活跃负载与连续性；非空能力要求不会进入通用项目兜底。任职中的非空能力优先于角色档案。主 Agent 核实了上述调用链；显式指定角色和已有任务身份复用保持独立语义。未因日常服务的一次不同路由结果就认定当前源码同样有缺陷，运行实例是否加载当前源码仍需实际更新/验收证据。多能力及任职覆盖档案的真实组合场景仍有覆盖空间。
- 后续已补负责人组合的真实 HTTP/Neo4j 验收：9 项新用例覆盖多能力全集、任职/档案优先级、工作类型/负载/历史排序、成功优先于最近失败、显式指定与会话保留语义。主 Agent 独立实现并运行；原生 worker 只读提供四组用例建议，没有运行测试，也不替代 Claude 审查。本轮没有发现需要修改生产逻辑的新缺陷，未重复启动已结束的 Claude 502 调用。
- 新一轮外部执行器预检仍通过，但 Fuli 的 degraded 健康门禁未派发审查 worker。另按执行器预检流程运行一次真实只读健康 smoke，10 次 API 重试后仍返回上游 502，没有执行 Read；进程已回收并上报 degraded。没有修改登录或网关，也没有绕过门禁将 smoke 记成代码审查。
- 原生记忆事务核对 worker 完成限定文件只读检查，确认当前 prepare 与记忆同事务、锁顺序、授权及幂等边界；未运行测试。主 Agent 已独立完成真实 HTTP/Neo4j 回归。成功 prepare 后后续提炼失败可能保留已提交记忆，这是当前明确的提交边界，不宣称整个结束工作流原子化。worker 完成状态已上报、租约已释放，仍不等于 Claude clean。

实际 Claude 响应元数据报告 `claude-opus-5`。所有外部审查均只读，禁用写入、命令、嵌套委派和自定义 MCP/hooks；不能据此声称完成了真实产品 hooks 验收。

## 宿主加载核实（2026-09-01）

真实 Codex CLI 0.149.1 的 app-server 只读 `hooks/list` 检查已完成：当前工作区与
仓库两种 cwd 下，均没有返回 Fuli MCP hooks，错误和警告计数均为零。检查只调用
initialize 和 hooks/list，没有启动模型任务、执行 hooks、修改配置或信任；诊断进程正常退出。
这只证明该 CLI 配置下未加载 Fuli hooks，不代表已检查桌面应用所有进程或其他宿主。

当前会话暴露的 Fuli 工具定义也未包含新增的角色工作记忆接口；该观察不能单独确定
运行中服务的版本。更新本机实现、刷新客户端连接并重新核实工具列表后，才能继续实际验收。
初次检查时尚未执行日常安装更新或服务重启，因可能影响其他会话而等待用户确认。hooks 信任仍须
用户审阅，不能由安装成功或配置文件存在推导。参见 [Codex hooks](https://learn.chatgpt.com/docs/hooks)。

随后用户明确授权备份、更新和重启，实际本机部署已完成：先备份旧全局包、Provider
虚拟环境、运行配置和三端配置；停止服务后复制原生数据库，并对原件与副本做完整内容摘要
比对。使用工作区构建的本地 npm 包和 Python wheel 离线安装，没有提交、上传或发布代码。
由于版本号相同，原生 Provider 安装缓存不会自行更新源码，本次显式重装该包，未升级
Python 依赖。三端仅刷新 Fuli 的连接、引导、hooks 和随包 Skills；未修改 hooks 信任。

部署后的 22 项检查通过：200 个安装后的 Node/Web 文件及 67 个 Provider Python 源码
文件与工作区一致；运行设置、图身份及连接配置、Provider 环境保持原内容；非 Fuli 配置
与 hooks 均保留。新启动的安装版 MCP 提供角色工作记忆工具及新字段，在线 Provider
暴露记忆路由，控制台返回当前构建。原有共享 Provider 不可用状态没有被本次更新修复。

复查真实 Codex CLI：工作区和仓库均加载了 Fuli 的 UserPromptSubmit 与 Stop 两项，
状态为 enabled、untrusted。因此部署通过不等于自动入口通过；须用户在 `/hooks` 审阅并
信任后继续普通输入验收。已打开的客户端会话仍须重连或新建任务以加载更新后的工具定义。
后续只读复查仍为相同的 enabled、untrusted 状态；未执行 hooks 或代用户设置信任。
更新前正式 Node 全量仍为 677 通过、1 跳过，Web 类型检查与生产构建均通过。

负责人组合验收后的部署复核不能复用上述“22 项全通过”结论：未持有租约的直接
Provider 健康请求先遇到连接拒绝；正常获取图租约后服务重新可用，记忆路由检查通过，
67 个 Provider 源文件仍与工作区一致。当前工作区的静态资源缓存头与 Web 构建已有
并发改动，因此不再与已安装的 Node/Web 全部一致；本轮未覆盖或再次部署这些改动。
Claude 配置与备份的差异仅在 `cachedGrowthBookFeatures` 和对应时间缓存字段，
并非 Fuli MCP、hooks 或其他连接配置丢失。验证租约已释放。
随后新增的 `src/server.js` 启动构造失败清理修复也仅在工作区，尚未再次部署。
最新只读 hooks 检查仍为两项 enabled、untrusted；未代用户设置信任。
其后新增的检查准备/记忆原子性修复同样未部署，要求 Provider 与 MCP 协同更新。

随后继续按用户已给的备份更新授权，完成第二次有界本机更新：先比较安装实物，发现
5 个 Node 源文件与 2 个 Provider 源文件不同，依赖清单一致，工作区 Web 另有并行改动。
以已安装包为基线制作隔离候选，保留 Web 构建，仅加入上述 7 个实现文件及角色记忆说明。
候选最初未加入静态缓存头文件，54 项回归中两项因此失败；核实是已有缓存修复后纳入，
重跑 54 项全部通过，6 项真实 HTTP/Neo4j/MCP 验收也全部通过。没有删除或放宽测试。
Provider 候选默认全量为 326 通过、33 跳过、192 项既有警告；原有协程清理警告仍保留。

打包前先核验内容白名单：本地 npm 包 287 个文件与候选一致，wheel 67 个 Python 文件
一致，差异恰为 7 个实现文件与 1 份说明；未包含测试、虚拟环境或本机绝对路径。
构建先因 npm 空配置路径重复、Python 缺少构建后端失败，两者均发生在安装前；分别改用
两个独立空 npm 配置和临时隔离构建依赖后成功，没有更改运行时依赖。

确认运行时没有活跃租约及托管 worker 后停止服务，完成独立离线数据库备份与摘要核验，
再安装本地 npm 包并强制重装同版本 Provider wheel、刷新三端配置和启动服务。
备份目录权限为 0700；未覆盖前一轮备份。安装后 23 项检查通过，200 个 Node/Web 文件
与 67 个 Provider 源文件逐一匹配的是该隔离候选；在线 Provider 已暴露原子记忆字段，
新启动的安装版 MCP 已暴露相应工具。其他 MCP/hooks、图身份和运行配置保持不变。
91 个顶层 npm 包及 39 个 Python distribution 的版本检查没有变化；未上传或发布。

真实 Codex CLI 的只读 hooks 检查仍返回两项 enabled、untrusted，两个 cwd 各有一项
警告、零错误；进程已结束，未执行 hooks。客户端配置未变化时安装器给出的
`newTaskRequired=false` 仅说明配置层无需重建，并不是对既有 MCP 进程热重载的证明。
尝试通过 Computer Use 检查 Codex 的重连入口时，工具明确拒绝操控该应用；没有尝试
更换方式绕过该限制。当前客户端重连与 hooks 信任仍需用户处理。

原生 worker 完成普通输入验收的只读证据核对，主 Agent 已复核调用点：现有真实 CLI
runner 显式提示 MCP 调用且关闭 hooks，真实数据库测试同样直接调用工具，均不能充当
普通输入自动唤醒证据。Stop 仅校验 checkpoint，实际记忆写入依赖后续模型调用；后续验收
须区分宿主 hook 与模型主动调用，核对事件顺序，再独立读回 Provider。该复核没有运行
Claude Code 或修改生产逻辑，也没有产生新的 Claude clean 结论。

## 当前回归证据

| 检查 | 结果 |
| --- | --- |
| 运行时/租约与 HTTP 请求边界回归 | 上一轮 40 项通过；此前新增 5 项 HTTP/service 用例已纳入 Node 全量，最新构造失败/关闭/监听专题 11 项通过；新增用例观察原生 SQLite I/O，无业务服务替身 |
| 宿主配置、生成指令、工具表面与文档回归 | 42 项通过 |
| Codex hooks 安装交付提示、配置与 bootstrap 回归 | 最新 24 项通过，含新增首次/重复安装两项公开 CLI 回归；图运行时启动为替身，文件安装真实且隔离 |
| 角色包安装 CLI 与已有包回归 | 最新 6 项通过；两项新增均先失败后通过，使用真实 Node CLI 与临时文件，无数据库或模型替身 |
| 负责人任务协调回归 | 最新 32 项通过；保留现有单测的驱动/候选替身边界，不等于已验证日常运行实例 |
| Python 默认全量 | 最新 326 项通过、33 项条件跳过，新增 5 项任务记忆模型校验；192 项依赖/既有测试夹具警告，另有清理时未 await 协程警告。既有活动驱动夹具支持事务端口，断言改为定位实际事件写入而非固定查询序号 |
| 显式启用的隔离 Neo4j 持久化与负责人套件 | 最新 30 项通过；此前记忆/任务/活动原子性 21 项加负责人组合 9 项。保留 7 项依赖 schema 警告；成功/失败历史为合成报告，不代表真实模型执行 |
| 真实 TCP HTTP Provider + 独立 MCP 接续及记忆准备原子性 | 最新 6 项通过：原有 3 个来源标识、9 个独立进程接续，加 5 项准备事务用例；不是实际产品会话。竞争完成/替代、CAS 回滚及合并、响应丢失后跨应用重试、客户端授权均已核验 |
| 真实 Claude Code → 全新 Codex CLI | 独立 TCP Provider / Neo4j 中通过；随机摘要不在任务提示里，精确摘要与版本 1 → 2 → 3 均由公开 HTTP 核验；两端均为 engineer，使用的都是合成数据 |
| Node 全量 | 主 Agent 最新按 `npm run test:node -- --test-concurrency=2 --test-reporter=spec` 正式入口运行，685 项中 679 项通过、6 项条件跳过；6 项真实 Neo4j/HTTP 验收另行启用均通过。此前启动清理修复后的 679 通过/1 跳过已由原生 worker 独立重跑，此后新增 5 项没有冒称由 worker 运行。包括示例模块与同期并发改动；仅 `test/*.test.js` 的旧结果不作为包含示例的全量证据 |
| Web 类型检查和全量 | 本轮类型检查通过；全量最初 206 通过/1 失败，已定位为过宽 DOM 断言；修正后全量 53 个文件、207 项通过，未修改界面行为 |
| 差异空白检查 | `git diff --check` 通过 |

HTTP 租约测试中的时钟、图服务启动和状态存储为测试替身；关闭测试的临时 Node 进程、断连测试的 TCP HTTP 和持久化验收的 Neo4j 是真实对象。数据库故障与目标缺失在外部驱动边界注入，未删除日常数据。Neo4j 验收使用临时样本，服务已停止；来源标识为 Codex、Claude Code、Cursor 的协议进程，不是三个已登录产品会话，也不是云端连接验收。

另行完成的真实产品验收使用两个真实 CLI 进程，不是更换来源标签的协议替身。Claude 实际响应
模型为 `claude-opus-5`；Codex 使用委派策略指定的 Luna 参数，但 JSONL 未提供响应模型，
不把请求参数当作实际模型证明。Claude 最终文字曾把 engineer 拼成 enginer；通过结论依据
实际工具上下文和 HTTP 持久结果，不依据模型自报的成功句子。首次失败分别保留为：并行测试
误共用图导致管理员令牌轮换、隔离环境遗漏代理、模型容量不足和 checkpoint 审批拒绝。
最终仅对本次隔离样本的 checkpoint 工具配置许可，未绕过 sandbox 或 hooks 信任。
此项没有证明普通输入自动触发，也未测试 Cursor 和云端；复跑任务原子性 5 项、文档 8 项通过。

## 新一轮候选的主 Agent 核验

- 记忆 R3 的 `active_task_owner` 换人候选不成立：`_select_agents` 对显式 lead 只返回该身份或抛错，不会回退到另一个人。这是此前已核对过的上层约束，不能因本轮未读该文件而重新当作确定问题。
- 记忆 R3 的来源字段伪装候选未证明额外授权绕过：Provider 仍验证空间 reader 权限，`source_application` 是客户端归属元数据，并非第二套身份凭据；托管 MCP 读记忆还校验角色与来源。不能用重复检查同一个自报字段代替真正的身份绑定。该更强隔离需求需要独立威胁模型，未擅自改变认证契约。
- 记忆 R3 的两类 409 文案合并属于诊断改进，未证明错误成功或数据损坏；同一错误已明确包含冲突/被替代两种情况。维持现有契约。
- 运行时 R2 关于 native 缺少 `ready()` 的前提不成立：`src/native-runtime/runtime.js` 实现并导出了 `ready()`。默认容器实现也具备该方法。
- 运行时 R3 对“停机可能唤醒容器”作了相互冲突的排除/待查描述。主 Agent 读取 `ensureContainerRuntime` 后确认停止路径确实能启动桌面引擎，已通过边界回归修复；未实际启动用户 Docker/Rancher。
- 运行时 R3 的显式关闭失败已通过公共接口复现并修复；外部 executor 历史条目无界保留、异常租约协议响应与持续停机失败策略仍待核验；较长冷启动预算未做真实耗时验收。
- HTTP R1 的 EmployeeError 任意状态码前提未成立：构造器默认 400；既有构造点用常量，动态工具错误先检查整数且限制 400–499。不能凭 `error.status` 未重复检查就称远程可触发进程退出。
- HTTP R1 的正常监听后 authority 赋值竞态未证明：监听事件 resolve 后的赋值在连续的 Promise 微任务中完成，中间没有网络事件可插入的 await；建议 `ref/unref` 也不能作为正确的连接接纳控制。保留现有 fail-closed 边界。
- HTTP R1 的 malformed lease URI→500、启动失败后 system 清理、响应发出后清理失败的兜底写入均已先复现再修复。TypeError/SyntaxError 的广义映射、固定 LAN 地址和 localhost 限制作为现行契约记录，不擅自改鉴权边界。
- 历史缺边复审的缺 `status` / `created_at` 意见目前只覆盖外部损坏的数据：当前创建查询在同一个 `ON CREATE SET` 内写入两字段；旧式逐查询提交不会只提交半个 SET。此处没有为了满足假设而放宽合法任务契约。招聘字段意见须结合招聘流程继续核对。

## 未完成事项

1. 核实全部候选反馈，对有效问题补回归、修复，并再次取得 Claude 复审报告。
2. 继续覆盖尚未形成报告的 HTTP/MCP、知识工作流、Provider、备份/安装、Web 和示例模块，不以已完成区块替代全面范围。
3. 全面审查期间应持续核对并发工作树变化；角色页面已局部提取模块并通过验证，其他任务的改动未还原。
4. 员工安装 CLI 已开始用真实子进程和临时目录验收；员工 HTTP API 及剩余候选仍待验证，不能据此扩大为全部员工功能通过。
5. 更新后的真实宿主连接、hooks 信任与普通输入自动触发，仍需实际产品验收。Cloud 的具体入口也待明确。
6. 活动审计核心与独立 actual/outcome 均已在链路恢复后取得限定范围 clean，不再把旧 502 当成当前阻碍。旧版已提交事件但缺审计的数据不会自动修补，不用当前执行器状态或推测补造历史执行证据。其他并发写入仍需分区检查。

安装器收据符号链接风险已从源码推导转为真实 CLI 复现并修复，详见上表；
该回归只触碰可丢弃样本，不代表检查了历史安装包。随后完成的日常安装更新另见宿主加载核实。
