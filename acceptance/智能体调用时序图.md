# Fuli 智能体调用时序图

## 实现口径

Fuli 不会由服务端无条件把全部知识推给模型。任务入口只组装当前实际生效的个人全局偏好和
精确项目偏好，普通历史知识由 Agent 按任务需要检索。Claude Code 通过入口与 Stop Hook
执行确定性生命周期检查；Codex、Cursor 等当前依靠 Prompt fallback，遵循相同契约，但宿主
不能保证一定阻止 Agent 漏掉末尾检查。

图中普通路径为已实现；橙色区块是 🟠 Beta；灰色区块是 ⚪ TODO。外部知识库在所有路径中
都只允许读取，`mirror` / `hybrid` 的“同步”只会在本地个人项目形成受限、待确认的镜像。

```mermaid
sequenceDiagram
    autonumber
    actor U as 用户
    participant A as Agent
    participant L as Agent 生命周期接入
    participant T as Fuli 任务入口
    participant P as Fuli 偏好入口
    participant K as Fuli 知识入口
    participant X as Fuli 外部知识网关
    participant R as "三方知识库（只读）"
    participant S as "公共空间（Beta）"
    participant M as Fuli 项目管理入口
    participant G as Graphiti 图服务
    participant N as Neo4j 图数据库

    U->>A: 在项目甲中提交任务
    alt Claude Code：Hook 强制
        L->>T: UserPromptSubmit 调用 begin_task_context
        T->>P: 读取协作偏好（当前工作目录）
        T->>T: 建立本次任务令牌
    else Codex / Cursor：Prompt fallback
        A->>P: 读取协作偏好（当前工作目录）
    end
    P->>G: 识别当前个人空间和项目甲
    G->>N: 查询个人全局偏好和项目甲偏好
    N-->>G: 返回候选偏好与冲突
    G-->>P: 只保留当前有效偏好
    Note over P,A: 注入个人全局偏好与项目甲偏好<br/>不注入项目乙偏好<br/>不自动注入待确认偏好
    P-->>A: 返回有效偏好、精确项目和待处理冲突

    opt 用户已为当前项目配置 mirror / hybrid，并手动触发同步
        U->>X: 在连接页检查并同步绑定
        X->>R: 只读分页获取文档
        R-->>X: 返回稳定 ID、正文、来源和游标
        X->>X: 规范化、哈希去重和分块
        X->>G: 写入当前个人项目的外部镜像
        G->>N: 保存 observed、pending、restricted、local_only 知识
        Note over X,R: 不存在对第三方知识库的写操作
    end

    opt 当前任务确实需要某项延迟冲突偏好
        A->>P: 比较双方证据并提交解决理由
        P->>G: 保存人工授权下的 AI 解决结果
        G->>N: 保留修订历史、解决者和判断依据
        P-->>A: 返回新的有效偏好
    end

    alt 当前任务可能依赖历史知识
        alt 当前项目有实时外部绑定，或明确选择公共项目
            A->>X: search_connected_knowledge（焦点查询）
            par 个人项目与获授权上级
                X->>K: 执行有界图谱检索
                K->>G: 构造个人项目作用域
                G->>N: 查询已确认、智能体已确认和待确认知识
                N-->>G: 返回正文、状态、历史和来源
                G-->>K: 本地同稳定键覆盖继承知识并排序
                K-->>X: 返回个人图谱证据
            and 三方知识库
                X->>R: 对 live / hybrid 绑定执行只读 retrieve
                R-->>X: 按绑定分别返回正文与来源
            and 明确选择的公共项目
                rect rgb(255, 243, 214)
                    X->>S: 查询调用方明确传入的公共项目 ID
                    S-->>X: 返回订阅与权限边界内的证据（Beta）
                end
            end
            X-->>A: 分开返回图谱、各外部绑定、错误和冲突策略
            alt Agent 发现会影响当前回答的实质冲突
                alt 策略为询问用户（默认）
                    A-->>U: 并列冲突正文、时间、作用域和来源，请求选择
                    U-->>A: 给出当前任务判断
                else 策略为 Agent 本次判断
                    A->>A: 只为本次回答选择，并解释判断依据
                    Note over A,X: 不确认、不失效、不改写任何来源
                end
            else 未发现实质冲突
                A->>A: 按来源与权威边界使用证据
            end
        else 只需要 Fuli 本地历史知识
            A->>K: 按焦点问题检索当前项目知识
            K->>G: 构造有界检索作用域
            Note over K,G: 先查项目甲本地知识<br/>再沿 PART_OF 或 USES_KNOWLEDGE_FROM 向上查找<br/>最多两跳，RELATED_TO 不扩张范围
            G->>N: 查询本地知识与允许继承的上级知识
            N-->>G: 返回已确认、智能体已确认和待确认知识
            G->>G: 本地同稳定键知识覆盖继承知识
            G->>G: 按权威性、相关性和作用域距离排序
            alt 命中支持当前回答的知识
                G-->>K: 返回正文、状态、双分数、历史和来源标记
                K-->>A: 提供模型可见的有界知识证据
            else 未命中
                G-->>K: 返回顶部未命中标记和空页脚
                K-->>A: 只提供一次未命中链接和后续检索指引
            end
        end

        alt 个人图谱知识实际影响回答、实现或决策
            A->>K: 记录一次引用或应用
            K->>G: 按知识、任务、使用类型和内容代次去重
            G->>N: 写入唯一有效使用事件
            G->>G: 更新效用分数和置信分数
            alt 达到五次且跨三个任务，并且没有未解决冲突
                G->>N: 晋级为智能体已确认
            else 未达到门槛或存在冲突
                G->>N: 保持原确认状态
            end
            K-->>A: 返回计数和晋级结果
        else 仅检索、查看，或只使用实时外部证据
            Note over A,K: 不记录个人图谱有效使用，不增加确认权重
        end
    else 当前任务不依赖历史知识
        Note over A,K: 不调用知识检索，也不为“有沉淀”而制造知识
    end

    A->>A: 完成回答、实现与验证

    opt 本次形成了明确决策
        A->>K: 保存候选方案、人的选择、理由和可选初始验证
        K->>G: 创建 Decision、DecisionOption 和 DecisionRationale
        G->>N: 保留确认权威与 VALIDATED_BY 关系
        Note over A,K: 当前尚无针对既有 Decision 单独追加验证的专用入口
    end

    opt 已使用知识被拒绝、反证、验证失败或过时
        A->>K: 记录知识反馈及证据
        K->>G: 按任务、内容代次和事件类型去重
        G->>N: 保存负面证据并标记需要复核
        Note over G,N: 降权不删除历史<br/>Agent 证据不能静默取消人工确认
    end

    opt 当前任务明确需要修改项目关系或项目资料
        A->>M: 预览项目写入（提交完整写入意图）
        M->>G: 验证项目、关系、冲突和目标
        G-->>M: 返回预览结果
        M-->>A: 返回短期、一次性预览令牌
        A->>M: 使用同一完整动作和预览令牌执行
        M->>M: 校验令牌匹配、有效且未使用
        M->>G: 执行项目写入
        G->>N: 保存变更
        M-->>A: 令牌失效并返回结果
    end

    A->>T: checkpoint_task_knowledge
    alt 有少量、稳定、可复用的候选
        T->>K: capture_candidates
        K->>G: 按证据、作用域和权威保存候选
        G->>N: 写入结构化知识，不保存原始会话
    else 没有值得长期保存的内容
        T-->>A: retain_nothing
    end

    alt Claude Code Stop Hook
        L->>T: verify_task_checkpoint
        alt 入口成功但尚未完成检查
            T-->>L: 阻止结束并要求完成 checkpoint
        else 已 capture_candidates 或 retain_nothing
            T-->>L: 允许结束
        end
    else Prompt fallback Agent
        Note over A,T: Agent 应执行相同末尾检查<br/>但宿主没有确定性 Stop Gate
    end

    A-->>U: 按有效偏好和支持性知识完成回答
    Note over A,U: 命中时保留顶部标记和底部来源<br/>未命中时只显示顶部标记，不重复页脚

    rect rgb(242, 243, 243)
        Note over X,S: TODO：外部绑定直接指向公共空间、定时/Webhook 增量同步、<br/>外部候选经人工审核和预览令牌提升到公共空间
    end
```

## 与项目理念的对应关系

1. **不是全部注入**：入口只加载有效偏好，详细知识按需检索。
2. **子项目优先**：先查当前项目，再向明确授权的上级查找共享 Runbook；同键内容以子项目
   为准。
3. **使用不等于看过**：只有真正影响结果才记录使用，检索本身不提高置信度。
4. **人保留最终权威**：Agent 发现候选和处理已授权冲突，但不能伪造人工确认。
5. **不是每次都沉淀**：`capture_candidates` 与 `retain_nothing` 都是完整、正确的任务结尾。
6. **历史不会被静默覆盖**：决策理由、修订和负面证据都保留可追溯关系。

## 关键检查点

1. Claude Code 的 `UserPromptSubmit` 入口和 Stop 检查属于 Hook 强制；其他 Agent 必须标为
   Prompt fallback。
2. 项目甲不会自动携带项目乙偏好；项目级个人偏好也不从父项目向下继承。
3. 待确认普通知识可以按需检索，待确认偏好不能自动注入。
4. 项目继承只沿 `PART_OF` / `USES_KNOWLEDGE_FROM`，最多两跳；`RELATED_TO` 不扩张范围。
5. 最终回答只能引用模型实际看到的知识正文，不能只凭来源标题猜测。
6. 只读任务不得尝试调用项目写入工具；项目写入必须先预览并使用匹配的一次性令牌。
7. 公共知识候选是启发式提示；没有人工选择和预览令牌就不能提升到上级项目。
8. 当前 `record_decision_trace` 支持首次写入的可选验证，但 Benchmark 要求的既有决策后续
   验证入口仍待实现。
9. 未命中回答只显示一次顶部来源标记，底部未命中页脚必须为空。
10. 三方连接器只能读；本地镜像统一进入绑定的个人项目，并保持 `pending`、`restricted`。
11. `ask_human` 是默认冲突策略；`agent_decide` 只授权本次回答，不能产生隐式知识修订。
12. 明确选择公共项目参与聚合检索是 Beta；外部知识直接绑定或自动提升到公共空间仍是 TODO。
13. 当前同步由连接页或 API 手动触发；定时任务、Webhook 和完整删除对账不得描述为已实现。
